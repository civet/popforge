Copy "init.8bitboy.xml" and rename it to "8bitboy.xml",
which is the actual file loaded into the 8BitBoy Player.

"8bitboy.xml" is added to the ignore list to avoid merges while developing.
	(As long the config structure itself do not change!)